# Method (extend here)

- Align with `HttpMethod` in `../interface.ts`.
- Map methods to k6 calls in `../../../generator/index.ts` (`http.get`, `http.post`, …).
- Later: JSON body, query string, multipart, etc.
